# `profiles/hardware/` — OBU / RSU / backend hardware profiles

Placeholder. YAML profiles with a citation per number (CPU and HSM service
times, queue depths, storage limits), consumed by `v2xw-node`.
