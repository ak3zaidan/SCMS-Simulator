# `scenarios/` — scenario files (YAML/JSON, schema v1)

Placeholder. Templates land here: `single-intersection`, `downtown-1km2`,
`highway-10km`, `district`, plus the migrated legacy presets from
`legacy/saved_scenarios/` (02-architecture.md §10).
