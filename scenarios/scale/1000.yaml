# The 1000-vehicle rung of the scaling ladder: the ADR 0011 high-tier target density.
#
# `max_total_vehicles` is a cap on *trips*, and the lane-insertion gap turns it into a
# count of vehicles; the run report states what it realised. See `base.yaml` for the method
# and for what the bulk spawn costs in realism.
schema: v2xw/scenario/1
meta:
  name: scale-1000
  base: base.yaml
  description: The scaling ladder at a nominal 1000 vehicles.
time:
  duration_s: 2.0
actors:
  vehicles:
    demand:
      params:
        max_total_vehicles: 1600
