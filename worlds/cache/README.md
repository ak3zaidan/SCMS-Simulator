# `worlds/cache/` — content-addressed world builds

Build output, git-ignored except for this file. A world build is keyed by the
hash of its sources and import settings, so a rebuild is a cache hit
(02-architecture.md §10).
